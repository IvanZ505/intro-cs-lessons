public class Anagrams {
    public static boolean anagram(String s, String comparison) {
        if(s.length() != comparison.length()) {
            return false;
        }
        char[] s_array = new char[s.length()];
        char[] comp_array = new char[comparison.length()];
        return true;

    }
}
